services:
  wow3-renderer:
    build:
      context: ../..
      dockerfile: apps/wow3-renderer/Dockerfile
    ports:
      - "0.0.0.0:${PORT:-4000}:${PORT:-4000}"
    volumes:
      - renderer-data:/data
    env_file:
      - .env
    restart: unless-stopped

volumes:
  renderer-data:
